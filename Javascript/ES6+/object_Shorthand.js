//if variable name and object key is same in name we can just write a name in object instead of using key value pair in object`


let name1 ="naeem"
let age=23

let obj={
    name1,
    age,
}
console.log(obj)